# Repository Guidelines

このファイルの指示はリポジトリ全体に適用する。より深い階層に `AGENTS.md` がある場合は、その階層ではそちらを優先する。

## 作業フロー

- 作業は対応するGitHub Issueを確認してから始める。
- `main` に直接実装をコミットしない。最新の `main` から `codex/*` ブランチを作成する。
- 原則として1つの作業Issueに対して1つのPull Requestを作成する。
- Pull Request本文に `Closes #<Issue番号>` を記載する。
- コミットメッセージは `docs:` や `feat:` などの種別を付け、説明部分を日本語で記載する。
- Pull Requestはユーザーのレビュー後にMerge Commitで取り込む。Squash MergeとRebase Mergeは使用しない。
- 親ブランチから作業ブランチを派生させず、前のPull Requestがマージされた後に最新の `main` から次のブランチを作成する。

## 変更時の注意

- Issueの目的に関係しない変更を行わない。
- ユーザーによる既存の変更を無断で破棄、上書き、整形しない。
- 秘密情報、認証情報、実際の環境変数値をコミットしない。必要な変数は値を伏せて `.env.example` に記載する。
- READMEでは、実装済みの内容と予定している内容を明確に区別する。
- READMEに一時的な作業手順やCodex固有の運用を記載しない。

## APIクライアント生成

- Orvalの生成物を直接編集しない。
- Spring BootのOpenAPI仕様を変更した場合は、Orvalの生成コマンドを実行して生成物も更新する。
- 生成コマンド実行後に未反映の差分がないことを確認する。

## 検証

- 変更範囲に対応するテスト、lint、型チェック、buildを実行する。
- 実行できない検証がある場合は、理由と未確認範囲を報告する。
- コマンドの終了コードと出力を確認するまで、変更が完了したと報告しない。
